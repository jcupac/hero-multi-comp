services:

  backend:
    image: ghcr.io/jcupac/hero-multi-comp/backend:latest
    ports:
      - "8081:8081"
    environment:
      - TODOS_API_HOST=https://jsonplaceholder.typicode.com
    networks:
      - app-network

  frontend:
    image: ghcr.io/jcupac/hero-multi-comp/frontend:latest
    ports:
      - "8080:8080"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge